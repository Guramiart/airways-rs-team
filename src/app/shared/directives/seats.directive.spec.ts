import { SeatsDirective } from './seats.directive';

describe('SeatsDirective', () => {
  it('should create an instance', () => {
    const directive = new SeatsDirective();
    expect(directive).toBeTruthy();
  });
});
